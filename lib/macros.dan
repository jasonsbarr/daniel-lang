(begin-module "macros"

(defmacro unless (pred a b)
    `(if ~pred ~b ~a))

(defmacro or (left right)
    `(if ~left ~left ~right))

(defmacro and (left right)
    `(if ~left ~right false))

(provide unless)
(provide or)
(provide and))
