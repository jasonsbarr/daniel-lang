(begin-module "core"

(defmacro unless (pred a b)
    `(if ~pred ~b ~a))

(defmacro or (left right)
    `(if ~left ~left ~right))

(defmacro and (left right)
    `(if ~left ~right false))

(define (not val)
    (if val false true))

(provide unless)
(provide or)
(provide and)
(provide not))
