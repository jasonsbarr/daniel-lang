(begin-module "list"
    (define (map fn lst)
        (for/list ((i lst))
            (fn i)))

    (define (filter pred lst)
        (for/list ((i lst) :when (pred i))
            i))

    (provide map)
    (provide filter))
